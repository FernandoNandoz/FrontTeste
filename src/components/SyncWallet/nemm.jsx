try {
    //const response = await api.post('/wallet', data)

    const dataUpdate = {
        //idWallet: response.data.id,
        //wallletAddress: response.data.walletAddress,
    }

    //const updateResponse = await api.put(`/user/updateUser/${userId}`, dataUpdate, {
    //    headers: {
    //        Authentication: `${token}`
    //    }
    // })

    // alert(`Usuário: ${response.data.walletAddress}, Connectado com sucesso!!!`);

    //console.log(response.data)
    //console.log(updateResponse.data)
    //console.log(dataUpdate)

} catch (err) {
    alert('Erro, tente novamente.');
}